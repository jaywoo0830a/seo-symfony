{% extends 'dashboard/layout.html.twig' %}

{% block title %}새 지역 · {{ parent() }}{% endblock %}

{% block content %}
    <div class="dashboard">
        <header class="dashboard__header">
            <a class="dashboard__back" href="{{ path('admin_regions') }}">← 지역 목록</a>
            <h1 class="dashboard__title">새 지역</h1>
        </header>

        {{ include('dashboard/regions/_form.html.twig', {submit_label: '생성'}) }}
    </div>
{% endblock %}
