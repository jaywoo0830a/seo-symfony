{% extends 'dashboard/layout.html.twig' %}

{% block title %}매트릭스 · {{ parent() }}{% endblock %}

{% block content %}
    <div class="dashboard">
        <header class="dashboard__header">
            <h1 class="dashboard__title">매트릭스</h1>
            <p class="dashboard__subtitle">테마 행 × 시도 열. 셀은 좌표별 콘텐츠 노드 상태입니다.</p>
        </header>

        <section class="dashboard__section">
            <table class="matrix">
                <thead>
                    <tr class="matrix__row">
                        <th class="matrix__cell matrix__cell--head matrix__cell--row-head">테마</th>
                        {% for sido in sidos %}
                            <th class="matrix__cell matrix__cell--head">{{ sido.name }}</th>
                        {% endfor %}
                    </tr>
                </thead>
                <tbody>
                    {% for row in matrix %}
                        <tr class="matrix__row">
                            <th class="matrix__cell matrix__cell--row-head">{{ row.theme.name }}</th>
                            {% for cell in row.cells %}
                                <td class="matrix__cell matrix__cell--{{ cell.state }}">
                                    {% if cell.state == 'empty' %}
                                        <span class="matrix__cell-label">·</span>
                                    {% else %}
                                        <span class="matrix__cell-figure">{{ cell.count }}</span>
                                    {% endif %}
                                </td>
                            {% endfor %}
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
            <ul class="legend">
                <li class="legend__item">
                    <span class="legend__swatch legend__swatch--live"></span>
                    <span>live</span>
                </li>
                <li class="legend__item">
                    <span class="legend__swatch legend__swatch--noindex"></span>
                    <span>noindex</span>
                </li>
                <li class="legend__item">
                    <span class="legend__swatch legend__swatch--empty"></span>
                    <span>빈 좌표</span>
                </li>
            </ul>
        </section>
    </div>
{% endblock %}
